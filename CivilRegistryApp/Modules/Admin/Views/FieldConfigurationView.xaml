<UserControl x:Class="CivilRegistryApp.Modules.Admin.Views.FieldConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CivilRegistryApp.Modules.Admin.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Field Configuration" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Document Type Selection -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Document Type:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="DocumentTypeComboBox" Width="200" SelectionChanged="DocumentTypeComboBox_SelectionChanged">
                <ComboBoxItem Content="Birth Certificate"/>
                <ComboBoxItem Content="Marriage Certificate"/>
                <ComboBoxItem Content="Death Certificate"/>
                <ComboBoxItem Content="CENOMAR"/>
            </ComboBox>
            <Button x:Name="RefreshButton" Content="Refresh" Margin="10,0,0,0" Padding="10,5" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- Field Configuration DataGrid -->
        <DataGrid Grid.Row="2" x:Name="FieldConfigDataGrid" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" Margin="0,0,0,10"
                  SelectionMode="Single" SelectionChanged="FieldConfigDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Field Name" Binding="{Binding FieldName}" IsReadOnly="True" Width="150"/>
                <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="150"/>
                <DataGridCheckBoxColumn Header="Required" Binding="{Binding IsRequired}" Width="80"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Display Order" Binding="{Binding DisplayOrder}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save Changes" Width="120" Height="30" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="ResetButton" Content="Reset to Default" Width="120" Height="30" Click="ResetButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
