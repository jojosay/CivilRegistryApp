<UserControl x:Class="CivilRegistryApp.Modules.Reports.Views.ReportGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CivilRegistryApp.Modules.Reports.Views"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="10" Background="{StaticResource PrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Report Type Selection -->
        <GroupBox Grid.Row="0" Header="Report Type" Margin="0,0,0,10">
            <StackPanel>
                <ComboBox x:Name="ReportTypeComboBox" Margin="0,5" SelectionChanged="ReportTypeComboBox_SelectionChanged">
                    <ComboBoxItem Content="Document Report" IsSelected="True"/>
                    <ComboBoxItem Content="Request Report"/>
                    <ComboBoxItem Content="Activity Report"/>
                </ComboBox>
            </StackPanel>
        </GroupBox>

        <!-- Report Parameters -->
        <Grid Grid.Row="1">
            <Grid x:Name="DocumentReportGrid" Visibility="Visible">
                <GroupBox Header="Document Report Parameters">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="Document Type:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="DocumentTypeComboBox" Margin="0,5">
                                <ComboBoxItem Content="All Types" IsSelected="True"/>
                                <ComboBoxItem Content="Birth Certificate"/>
                                <ComboBoxItem Content="Marriage Certificate"/>
                                <ComboBoxItem Content="Death Certificate"/>
                                <ComboBoxItem Content="CENOMAR"/>
                            </ComboBox>

                            <Label Grid.Row="1" Grid.Column="0" Content="Registry Office:" Margin="0,5" Foreground="White"/>
                            <TextBox Grid.Row="1" Grid.Column="1" x:Name="RegistryOfficeTextBox" Margin="0,5"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="Province:" Margin="0,5" Foreground="White"/>
                            <TextBox Grid.Row="2" Grid.Column="1" x:Name="ProvinceTextBox" Margin="0,5"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="City/Municipality:" Margin="0,5" Foreground="White"/>
                            <TextBox Grid.Row="3" Grid.Column="1" x:Name="CityMunicipalityTextBox" Margin="0,5"/>

                            <Label Grid.Row="4" Grid.Column="0" Content="Barangay:" Margin="0,5" Foreground="White"/>
                            <TextBox Grid.Row="4" Grid.Column="1" x:Name="BarangayTextBox" Margin="0,5"/>

                            <Label Grid.Row="5" Grid.Column="0" Content="Date From:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="5" Grid.Column="1" x:Name="DateFromPicker" Margin="0,5"/>

                            <Label Grid.Row="6" Grid.Column="0" Content="Date To:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="6" Grid.Column="1" x:Name="DateToPicker" Margin="0,5"/>

                            <Label Grid.Row="7" Grid.Column="0" Content="Export Format:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="7" Grid.Column="1" x:Name="ExportFormatComboBox" Margin="0,5">
                                <ComboBoxItem Content="PDF" IsSelected="True"/>
                                <ComboBoxItem Content="Excel"/>
                            </ComboBox>
                        </Grid>
                    </ScrollViewer>
                </GroupBox>
            </Grid>

            <Grid x:Name="RequestReportGrid" Visibility="Collapsed">
                <GroupBox Header="Request Report Parameters">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="Status:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="StatusComboBox" Margin="0,5">
                                <ComboBoxItem Content="All Statuses" IsSelected="True"/>
                                <ComboBoxItem Content="Pending"/>
                                <ComboBoxItem Content="Approved"/>
                                <ComboBoxItem Content="Rejected"/>
                                <ComboBoxItem Content="Completed"/>
                            </ComboBox>

                            <Label Grid.Row="1" Grid.Column="0" Content="Date From:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="1" Grid.Column="1" x:Name="RequestDateFromPicker" Margin="0,5"/>

                            <Label Grid.Row="2" Grid.Column="0" Content="Date To:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="2" Grid.Column="1" x:Name="RequestDateToPicker" Margin="0,5"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="Export Format:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="3" Grid.Column="1" x:Name="RequestExportFormatComboBox" Margin="0,5">
                                <ComboBoxItem Content="PDF" IsSelected="True"/>
                                <ComboBoxItem Content="Excel"/>
                            </ComboBox>
                        </Grid>
                    </ScrollViewer>
                </GroupBox>
            </Grid>

            <Grid x:Name="ActivityReportGrid" Visibility="Collapsed">
                <GroupBox Header="Activity Report Parameters">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="Activity Type:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="ActivityTypeComboBox" Margin="0,5">
                                <ComboBoxItem Content="All Types" IsSelected="True"/>
                                <ComboBoxItem Content="Login"/>
                                <ComboBoxItem Content="Logout"/>
                                <ComboBoxItem Content="Document Added"/>
                                <ComboBoxItem Content="Document Updated"/>
                                <ComboBoxItem Content="Document Deleted"/>
                                <ComboBoxItem Content="Request Created"/>
                                <ComboBoxItem Content="Request Updated"/>
                            </ComboBox>

                            <Label Grid.Row="1" Grid.Column="0" Content="User:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="UserComboBox" Margin="0,5">
                                <ComboBoxItem Content="All Users" IsSelected="True"/>
                            </ComboBox>

                            <Label Grid.Row="2" Grid.Column="0" Content="Date From:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="2" Grid.Column="1" x:Name="ActivityDateFromPicker" Margin="0,5"/>

                            <Label Grid.Row="3" Grid.Column="0" Content="Date To:" Margin="0,5" Foreground="White"/>
                            <DatePicker Grid.Row="3" Grid.Column="1" x:Name="ActivityDateToPicker" Margin="0,5"/>

                            <Label Grid.Row="4" Grid.Column="0" Content="Export Format:" Margin="0,5" Foreground="White"/>
                            <ComboBox Grid.Row="4" Grid.Column="1" x:Name="ActivityExportFormatComboBox" Margin="0,5">
                                <ComboBoxItem Content="PDF" IsSelected="True"/>
                                <ComboBoxItem Content="Excel"/>
                            </ComboBox>
                        </Grid>
                    </ScrollViewer>
                </GroupBox>
            </Grid>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="GenerateReportButton" Content="Generate Report" Width="140" Height="30" Padding="10,5" Margin="0,0,10,0" Background="#2E3A59" Foreground="White" Click="GenerateReportButton_Click"/>
            <Button x:Name="SaveAsScheduledButton" Content="Save as Scheduled" Width="140" Height="30" Padding="10,5" Background="#2E3A59" Foreground="White" Click="SaveAsScheduledButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
