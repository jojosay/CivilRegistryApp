<UserControl x:Class="CivilRegistryApp.Modules.Reports.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CivilRegistryApp.Modules.Reports.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource PrimaryBrush}">
    <Grid Background="{StaticResource PrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Reports" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" Foreground="White"/>
            <TabControl x:Name="ReportsTabControl" SelectionChanged="ReportsTabControl_SelectionChanged">
                <TabItem Header="Generate Reports">
                    <local:ReportGeneratorView x:Name="ReportGeneratorView"/>
                </TabItem>
                <TabItem Header="Scheduled Reports">
                    <local:ScheduledReportsView x:Name="ScheduledReportsView"/>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>
</UserControl>
